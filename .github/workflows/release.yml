name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: write # create the GitHub Release
  packages: write # push the image to GHCR

env:
  CARGO_TERM_COLOR: always

jobs:
  binaries:
    name: Build ${{ matrix.target }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # Linux targets build inside a rust:1-bookworm container so the
          # binary links against Debian bookworm's glibc — the same base
          # image the Docker runtime uses (see Dockerfile's runtime-prebuilt
          # stage). Building directly on the bare ubuntu-latest/-arm runner
          # links against whatever glibc that runner image ships (which
          # drifts forward independently, e.g. Ubuntu 24.04 -> glibc 2.39),
          # newer than bookworm's -> "GLIBC_2.39 not found" at container
          # runtime even though the binary runs fine on the runner itself.
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            container: rust:1-bookworm
          - target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04-arm
            container: rust:1-bookworm
          - target: x86_64-apple-darwin
            os: macos-15-intel
          - target: aarch64-apple-darwin
            os: macos-15
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container }}
    steps:
      - uses: actions/checkout@v6

      - name: Install build deps (Linux)
        if: runner.os == 'Linux'
        run: apt-get update && apt-get install -y --no-install-recommends cmake pkg-config perl make

      - name: Sync crate version to the tag
        shell: bash
        run: |
          VER="${GITHUB_REF_NAME#v}"
          perl -0pi -e 's/^version = "[^"]*"/version = "'"$VER"'"/m' Cargo.toml
          echo "Building version $VER"

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --features full --target ${{ matrix.target }}

      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          archive="leafmask-${GITHUB_REF_NAME}-${{ matrix.target }}.tar.gz"
          tar -C "target/${{ matrix.target }}/release" -czf "dist/${archive}" leafmask
          if command -v sha256sum >/dev/null; then sha256sum "dist/${archive}"; else shasum -a 256 "dist/${archive}"; fi \
            | awk -v f="${archive}" '{print $1"  "f}' > "dist/${archive}.sha256"

      - uses: actions/upload-artifact@v6
        with:
          name: leafmask-${{ matrix.target }}
          path: dist/*

  release:
    name: Publish GitHub Release
    needs: binaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v7
        with:
          path: dist
          merge-multiple: true
      - name: Aggregate checksums
        run: cat dist/*.sha256 > "leafmask-${GITHUB_REF_NAME}-checksums.txt"
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            dist/*.tar.gz
            leafmask-${{ github.ref_name }}-checksums.txt

  docker:
    name: Build & push image
    needs: binaries
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      # The binaries job already cross-compiled aarch64-unknown-linux-gnu
      # natively (on a real ARM runner) alongside x86_64-unknown-linux-gnu.
      # Reuse both instead of recompiling the full feature set from source
      # under QEMU emulation for the arm64 image — that step alone used to
      # take well over an hour.
      - uses: actions/download-artifact@v7
        with:
          path: dist
          merge-multiple: true
      - name: Stage per-arch binaries for the Docker build context
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p bin/amd64 bin/arm64
          tar -xzf "dist/leafmask-${GITHUB_REF_NAME}-x86_64-unknown-linux-gnu.tar.gz" -C bin/amd64
          tar -xzf "dist/leafmask-${GITHUB_REF_NAME}-aarch64-unknown-linux-gnu.tar.gz" -C bin/arm64

      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Lowercase owner for GHCR
        id: owner
        run: echo "name=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"
      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ steps.owner.outputs.name }}/leafmask
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest
      - uses: docker/build-push-action@v6
        with:
          context: .
          target: runtime-prebuilt
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
